# DEPRECATED — Caddy is no longer used in Phase 1.
# FullStop Phase 1 uses HestiaCP + Nginx for reverse proxy and TLS.
#
# See: config/nginx-proxy.tpl for the HestiaCP Nginx template.
# See: docs/architecture/DEPLOYMENT.md for full architecture.
#
# This file is retained for reference only. It will be removed when
# the Phase 2 Docker architecture is finalized.
